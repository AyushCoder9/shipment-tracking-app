# Leave empty in development — the Vite dev server proxies /api/* to localhost:4000.
# Set this in production (e.g. on Vercel) to the deployed API origin, no trailing slash.
# Example: VITE_API_BASE_URL=https://samex-api.onrender.com
VITE_API_BASE_URL=
