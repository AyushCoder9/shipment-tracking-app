PORT=4000
NODE_ENV=development
CLIENT_ORIGIN=http://localhost:5173

# Auth (single hardcoded user, see config/env.ts)
AUTH_USERNAME=admin
AUTH_PASSWORD=samex2026
JWT_SECRET=replace-me-with-a-long-random-string
JWT_EXPIRES_IN=24h

# Storage — leave MONGODB_URI empty to use the JSON-file repository.
MONGODB_URI=
